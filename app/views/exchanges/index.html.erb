<table class="table">
  <tr>
    <th><%= '依頼タイトル' %></th>
    <th><%= '応募コメント' %></th>
    <th><%= '応募ユーザ名' %></th>
    <th><%= '応募ユーザメール' %></th>
    <th><%= '詳細' %></th>
  </tr>
  <% @exchanges.each do |exchange| %>
    <tr>
      <td><%= exchange.request.title %></td>
      <td><%= exchange.content %></td>
      <td><%= exchange.user.name %></td>
      <td><%= exchange.user.email %></td>
      <td><%= link_to '詳細', exchange_path(exchange), class: 'btn btn-primary btn-block' %></td>
    </tr>
  <% end %>
</table>